 
 SELECT OBJ_ID, ATTCH_ID, ID_NUM, TITLE, TEXT_FILE,RELEVANCE() AS R, TABLE_NAME, TYPE_ID, TEXT_SRC 
 FROM strViewName 
 WHERE (TABLE_NAME CONTAINS strTableName) AND (TEXT_SRC CONTAINS ('strKey') |  ('strPhase'))
 ORDER BY strSortField strSortOrder;
